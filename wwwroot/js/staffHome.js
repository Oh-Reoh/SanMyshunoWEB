document.addEventListener('DOMContentLoaded', () => {
    console.log("Staff Home JS loaded — ready for future features!");

    // Future ideas:
    // - Scroll to load more posts
    // - Toggle expanded comment view
    // - Filter by category
});
